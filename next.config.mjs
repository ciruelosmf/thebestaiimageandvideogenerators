/** @type {import('next').NextConfig} */
const nextConfig = {

    images: {
        domains: ['images.ctfassets.net', 'framerusercontent.com'],
        // If you already have other domains, add to the existing array
        // domains: ['existing-domain.com', 'images.ctfassets.net'],
      },

};

export default nextConfig;
